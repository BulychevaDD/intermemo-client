import { DomainAuthenticationParameters } from './authentication.model';

export const mockDomainAuthenticationParameters: DomainAuthenticationParameters = {
  username: 'username',
  password: 'password',
};
